<?xml version="1.0" standalone="no"?><!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd"><svg version="1.1" width="1200" height="246" onload="init(evt)" viewBox="0 0 1200 246" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" xmlns:fg="http://github.com/jonhoo/inferno"><!--Flame graph stack visualization. See https://github.com/brendangregg/FlameGraph for latest version, and http://www.brendangregg.com/flamegraphs.html for examples.--><!--NOTES: --><defs><linearGradient id="background" y1="0" y2="1" x1="0" x2="0"><stop stop-color="#eeeeee" offset="5%"/><stop stop-color="#eeeeb0" offset="95%"/></linearGradient></defs><style type="text/css">
text { font-family:monospace; font-size:12px }
#title { text-anchor:middle; font-size:17px; }
#matched { text-anchor:end; }
#search { text-anchor:end; opacity:0.1; cursor:pointer; }
#search:hover, #search.show { opacity:1; }
#subtitle { text-anchor:middle; font-color:rgb(160,160,160); }
#unzoom { cursor:pointer; }
#frames > *:hover { stroke:black; stroke-width:0.5; cursor:pointer; }
.hide { display:none; }
.parent { opacity:0.5; }
</style><script type="text/ecmascript"><![CDATA[
        var nametype = 'Function:';
        var fontsize = 12;
        var fontwidth = 0.59;
        var xpad = 10;
        var inverted = false;
        var searchcolor = 'rgb(230,0,230)';
        var fluiddrawing = true;
        var truncate_text_right = false;
    ]]><![CDATA["use strict";
var details, searchbtn, unzoombtn, matchedtxt, svg, searching, frames, known_font_width;
function init(evt) {
    details = document.getElementById("details").firstChild;
    searchbtn = document.getElementById("search");
    unzoombtn = document.getElementById("unzoom");
    matchedtxt = document.getElementById("matched");
    svg = document.getElementsByTagName("svg")[0];
    frames = document.getElementById("frames");
    known_font_width = get_monospace_width(frames);
    total_samples = parseInt(frames.attributes.total_samples.value);
    searching = 0;

    // Use GET parameters to restore a flamegraph's state.
    var restore_state = function() {
        var params = get_params();
        if (params.x && params.y)
            zoom(find_group(document.querySelector('[*|x="' + params.x + '"][y="' + params.y + '"]')));
        if (params.s)
            search(params.s);
    };

    if (fluiddrawing) {
        // Make width dynamic so the SVG fits its parent's width.
        svg.removeAttribute("width");
        // Edge requires us to have a viewBox that gets updated with size changes.
        var isEdge = /Edge\/\d./i.test(navigator.userAgent);
        if (!isEdge) {
            svg.removeAttribute("viewBox");
        }
        var update_for_width_change = function() {
            if (isEdge) {
                svg.attributes.viewBox.value = "0 0 " + svg.width.baseVal.value + " " + svg.height.baseVal.value;
            }

            // Keep consistent padding on left and right of frames container.
            frames.attributes.width.value = svg.width.baseVal.value - xpad * 2;

            // Text truncation needs to be adjusted for the current width.
            update_text_for_elements(frames.children);

            // Keep search elements at a fixed distance from right edge.
            var svgWidth = svg.width.baseVal.value;
            searchbtn.attributes.x.value = svgWidth - xpad;
            matchedtxt.attributes.x.value = svgWidth - xpad;
        };
        window.addEventListener('resize', function() {
            update_for_width_change();
        });
        // This needs to be done asynchronously for Safari to work.
        setTimeout(function() {
            unzoom();
            update_for_width_change();
            restore_state();
        }, 0);
    } else {
        restore_state();
    }
}
// event listeners
window.addEventListener("click", function(e) {
    var target = find_group(e.target);
    if (target) {
        if (target.nodeName == "a") {
            if (e.ctrlKey === false) return;
            e.preventDefault();
        }
        if (target.classList.contains("parent")) unzoom();
        zoom(target);

        // set parameters for zoom state
        var el = target.querySelector("rect");
        if (el && el.attributes && el.attributes.y && el.attributes["fg:x"]) {
            var params = get_params()
            params.x = el.attributes["fg:x"].value;
            params.y = el.attributes.y.value;
            history.replaceState(null, null, parse_params(params));
        }
    }
    else if (e.target.id == "unzoom") {
        unzoom();

        // remove zoom state
        var params = get_params();
        if (params.x) delete params.x;
        if (params.y) delete params.y;
        history.replaceState(null, null, parse_params(params));
    }
    else if (e.target.id == "search") search_prompt();
}, false)
// mouse-over for info
// show
window.addEventListener("mouseover", function(e) {
    var target = find_group(e.target);
    if (target) details.nodeValue = nametype + " " + g_to_text(target);
}, false)
// clear
window.addEventListener("mouseout", function(e) {
    var target = find_group(e.target);
    if (target) details.nodeValue = ' ';
}, false)
// ctrl-F for search
window.addEventListener("keydown",function (e) {
    if (e.keyCode === 114 || (e.ctrlKey && e.keyCode === 70)) {
        e.preventDefault();
        search_prompt();
    }
}, false)
// functions
function get_params() {
    var params = {};
    var paramsarr = window.location.search.substr(1).split('&');
    for (var i = 0; i < paramsarr.length; ++i) {
        var tmp = paramsarr[i].split("=");
        if (!tmp[0] || !tmp[1]) continue;
        params[tmp[0]]  = decodeURIComponent(tmp[1]);
    }
    return params;
}
function parse_params(params) {
    var uri = "?";
    for (var key in params) {
        uri += key + '=' + encodeURIComponent(params[key]) + '&';
    }
    if (uri.slice(-1) == "&")
        uri = uri.substring(0, uri.length - 1);
    if (uri == '?')
        uri = window.location.href.split('?')[0];
    return uri;
}
function find_child(node, selector) {
    var children = node.querySelectorAll(selector);
    if (children.length) return children[0];
    return;
}
function find_group(node) {
    var parent = node.parentElement;
    if (!parent) return;
    if (parent.id == "frames") return node;
    return find_group(parent);
}
function orig_save(e, attr, val) {
    if (e.attributes["fg:orig_" + attr] != undefined) return;
    if (e.attributes[attr] == undefined) return;
    if (val == undefined) val = e.attributes[attr].value;
    e.setAttribute("fg:orig_" + attr, val);
}
function orig_load(e, attr) {
    if (e.attributes["fg:orig_"+attr] == undefined) return;
    e.attributes[attr].value = e.attributes["fg:orig_" + attr].value;
    e.removeAttribute("fg:orig_" + attr);
}
function g_to_text(e) {
    var text = find_child(e, "title").firstChild.nodeValue;
    return (text)
}
function g_to_func(e) {
    var func = g_to_text(e);
    // if there's any manipulation we want to do to the function
    // name before it's searched, do it here before returning.
    return (func);
}
function get_monospace_width(frames) {
    // Given the id="frames" element, return the width of text characters if
    // this is a monospace font, otherwise return 0.
    text = find_child(frames.children[0], "text");
    originalContent = text.textContent;
    text.textContent = "!";
    bangWidth = text.getComputedTextLength();
    text.textContent = "W";
    wWidth = text.getComputedTextLength();
    text.textContent = originalContent;
    if (bangWidth === wWidth) {
        return bangWidth;
    } else {
        return 0;
    }
}
function update_text_for_elements(elements) {
    // In order to render quickly in the browser, you want to do one pass of
    // reading attributes, and one pass of mutating attributes. See
    // https://web.dev/avoid-large-complex-layouts-and-layout-thrashing/ for details.

    // Fall back to inefficient calculation, if we're variable-width font.
    // TODO This should be optimized somehow too.
    if (known_font_width === 0) {
        for (var i = 0; i < elements.length; i++) {
            update_text(elements[i]);
        }
        return;
    }

    var textElemNewAttributes = [];
    for (var i = 0; i < elements.length; i++) {
        var e = elements[i];
        var r = find_child(e, "rect");
        var t = find_child(e, "text");
        var w = parseFloat(r.attributes.width.value) * frames.attributes.width.value / 100 - 3;
        var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
        var newX = format_percent((parseFloat(r.attributes.x.value) + (100 * 3 / frames.attributes.width.value)));

        // Smaller than this size won't fit anything
        if (w < 2 * known_font_width) {
            textElemNewAttributes.push([newX, ""]);
            continue;
        }

        // Fit in full text width
        if (txt.length * known_font_width < w) {
            textElemNewAttributes.push([newX, txt]);
            continue;
        }

        var substringLength = Math.floor(w / known_font_width) - 2;
        if (truncate_text_right) {
            // Truncate the right side of the text.
            textElemNewAttributes.push([newX, txt.substring(0, substringLength) + ".."]);
            continue;
        } else {
            // Truncate the left side of the text.
            textElemNewAttributes.push([newX, ".." + txt.substring(txt.length - substringLength, txt.length)]);
            continue;
        }
    }

    console.assert(textElemNewAttributes.length === elements.length, "Resize failed, please file a bug at https://github.com/jonhoo/inferno/");

    // Now that we know new textContent, set it all in one go so we don't refresh a bazillion times.
    for (var i = 0; i < elements.length; i++) {
        var e = elements[i];
        var values = textElemNewAttributes[i];
        var t = find_child(e, "text");
        t.attributes.x.value = values[0];
        t.textContent = values[1];
    }
}

function update_text(e) {
    var r = find_child(e, "rect");
    var t = find_child(e, "text");
    var w = parseFloat(r.attributes.width.value) * frames.attributes.width.value / 100 - 3;
    var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
    t.attributes.x.value = format_percent((parseFloat(r.attributes.x.value) + (100 * 3 / frames.attributes.width.value)));

    // Smaller than this size won't fit anything
    if (w < 2 * fontsize * fontwidth) {
        t.textContent = "";
        return;
    }
    t.textContent = txt;
    // Fit in full text width
    if (t.getComputedTextLength() < w)
        return;
    if (truncate_text_right) {
        // Truncate the right side of the text.
        for (var x = txt.length - 2; x > 0; x--) {
            if (t.getSubStringLength(0, x + 2) <= w) {
                t.textContent = txt.substring(0, x) + "..";
                return;
            }
        }
    } else {
        // Truncate the left side of the text.
        for (var x = 2; x < txt.length; x++) {
            if (t.getSubStringLength(x - 2, txt.length) <= w) {
                t.textContent = ".." + txt.substring(x, txt.length);
                return;
            }
        }
    }
    t.textContent = "";
}
// zoom
function zoom_reset(e) {
    if (e.tagName == "rect") {
        e.attributes.x.value = format_percent(100 * parseInt(e.attributes["fg:x"].value) / total_samples);
        e.attributes.width.value = format_percent(100 * parseInt(e.attributes["fg:w"].value) / total_samples);
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_reset(c[i]);
    }
}
function zoom_child(e, x, zoomed_width_samples) {
    if (e.tagName == "text") {
        var parent_x = parseFloat(find_child(e.parentNode, "rect[x]").attributes.x.value);
        e.attributes.x.value = format_percent(parent_x + (100 * 3 / frames.attributes.width.value));
    } else if (e.tagName == "rect") {
        e.attributes.x.value = format_percent(100 * (parseInt(e.attributes["fg:x"].value) - x) / zoomed_width_samples);
        e.attributes.width.value = format_percent(100 * parseInt(e.attributes["fg:w"].value) / zoomed_width_samples);
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_child(c[i], x, zoomed_width_samples);
    }
}
function zoom_parent(e) {
    if (e.attributes) {
        if (e.attributes.x != undefined) {
            e.attributes.x.value = "0.0%";
        }
        if (e.attributes.width != undefined) {
            e.attributes.width.value = "100.0%";
        }
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_parent(c[i]);
    }
}
function zoom(node) {
    var attr = find_child(node, "rect").attributes;
    var width = parseInt(attr["fg:w"].value);
    var xmin = parseInt(attr["fg:x"].value);
    var xmax = xmin + width;
    var ymin = parseFloat(attr.y.value);
    unzoombtn.classList.remove("hide");
    var el = frames.children;
    var to_update_text = [];
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        var a = find_child(e, "rect").attributes;
        var ex = parseInt(a["fg:x"].value);
        var ew = parseInt(a["fg:w"].value);
        // Is it an ancestor
        if (!inverted) {
            var upstack = parseFloat(a.y.value) > ymin;
        } else {
            var upstack = parseFloat(a.y.value) < ymin;
        }
        if (upstack) {
            // Direct ancestor
            if (ex <= xmin && (ex+ew) >= xmax) {
                e.classList.add("parent");
                zoom_parent(e);
                to_update_text.push(e);
            }
            // not in current path
            else
                e.classList.add("hide");
        }
        // Children maybe
        else {
            // no common path
            if (ex < xmin || ex >= xmax) {
                e.classList.add("hide");
            }
            else {
                zoom_child(e, xmin, width);
                to_update_text.push(e);
            }
        }
    }
    update_text_for_elements(to_update_text);
}
function unzoom() {
    unzoombtn.classList.add("hide");
    var el = frames.children;
    for(var i = 0; i < el.length; i++) {
        el[i].classList.remove("parent");
        el[i].classList.remove("hide");
        zoom_reset(el[i]);
    }
    update_text_for_elements(el);
}
// search
function reset_search() {
    var el = document.querySelectorAll("#frames rect");
    for (var i = 0; i < el.length; i++) {
        orig_load(el[i], "fill")
    }
    var params = get_params();
    delete params.s;
    history.replaceState(null, null, parse_params(params));
}
function search_prompt() {
    if (!searching) {
        var term = prompt("Enter a search term (regexp " +
            "allowed, eg: ^ext4_)", "");
        if (term != null) {
            search(term)
        }
    } else {
        reset_search();
        searching = 0;
        searchbtn.classList.remove("show");
        searchbtn.firstChild.nodeValue = "Search"
        matchedtxt.classList.add("hide");
        matchedtxt.firstChild.nodeValue = ""
    }
}
function search(term) {
    var re = new RegExp(term);
    var el = frames.children;
    var matches = new Object();
    var maxwidth = 0;
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        // Skip over frames which are either not visible, or below the zoomed-to frame
        if (e.classList.contains("hide") || e.classList.contains("parent")) {
            continue;
        }
        var func = g_to_func(e);
        var rect = find_child(e, "rect");
        if (func == null || rect == null)
            continue;
        // Save max width. Only works as we have a root frame
        var w = parseInt(rect.attributes["fg:w"].value);
        if (w > maxwidth)
            maxwidth = w;
        if (func.match(re)) {
            // highlight
            var x = parseInt(rect.attributes["fg:x"].value);
            orig_save(rect, "fill");
            rect.attributes.fill.value = searchcolor;
            // remember matches
            if (matches[x] == undefined) {
                matches[x] = w;
            } else {
                if (w > matches[x]) {
                    // overwrite with parent
                    matches[x] = w;
                }
            }
            searching = 1;
        }
    }
    if (!searching)
        return;
    var params = get_params();
    params.s = term;
    history.replaceState(null, null, parse_params(params));

    searchbtn.classList.add("show");
    searchbtn.firstChild.nodeValue = "Reset Search";
    // calculate percent matched, excluding vertical overlap
    var count = 0;
    var lastx = -1;
    var lastw = 0;
    var keys = Array();
    for (k in matches) {
        if (matches.hasOwnProperty(k))
            keys.push(k);
    }
    // sort the matched frames by their x location
    // ascending, then width descending
    keys.sort(function(a, b){
        return a - b;
    });
    // Step through frames saving only the biggest bottom-up frames
    // thanks to the sort order. This relies on the tree property
    // where children are always smaller than their parents.
    for (var k in keys) {
        var x = parseInt(keys[k]);
        var w = matches[keys[k]];
        if (x >= lastx + lastw) {
            count += w;
            lastx = x;
            lastw = w;
        }
    }
    // display matched percent
    matchedtxt.classList.remove("hide");
    var pct = 100 * count / maxwidth;
    if (pct != 100) pct = pct.toFixed(1);
    matchedtxt.firstChild.nodeValue = "Matched: " + pct + "%";
}
function format_percent(n) {
    return n.toFixed(4) + "%";
}
]]></script><rect x="0" y="0" width="100%" height="246" fill="url(#background)"/><text id="title" fill="rgb(0,0,0)" x="50.0000%" y="24.00">Flame Graph</text><text id="details" fill="rgb(0,0,0)" x="10" y="229.00"> </text><text id="unzoom" class="hide" fill="rgb(0,0,0)" x="10" y="24.00">Reset Zoom</text><text id="search" fill="rgb(0,0,0)" x="1190" y="24.00">Search</text><text id="matched" fill="rgb(0,0,0)" x="1190" y="229.00"> </text><svg id="frames" x="10" width="1180" total_samples="14492"><g><title>dyld`start (2 samples, 0.01%)</title><rect x="0.0000%" y="181" width="0.0138%" height="15" fill="rgb(227,0,7)" fg:x="0" fg:w="2"/><text x="0.2500%" y="191.50"></text></g><g><title>libsystem_malloc.dylib`_free (293 samples, 2.02%)</title><rect x="0.0138%" y="117" width="2.0218%" height="15" fill="rgb(217,0,24)" fg:x="2" fg:w="293"/><text x="0.2638%" y="127.50">l..</text></g><g><title>libsystem_malloc.dylib`_nanov2_free (46 samples, 0.32%)</title><rect x="2.0356%" y="117" width="0.3174%" height="15" fill="rgb(221,193,54)" fg:x="295" fg:w="46"/><text x="2.2856%" y="127.50"></text></g><g><title>libsystem_platform.dylib`_platform_memcmp (1,685 samples, 11.63%)</title><rect x="2.3530%" y="117" width="11.6271%" height="15" fill="rgb(248,212,6)" fg:x="341" fg:w="1685"/><text x="2.6030%" y="127.50">libsystem_platfor..</text></g><g><title>rust-1brc`DYLD-STUB$$free (81 samples, 0.56%)</title><rect x="13.9801%" y="117" width="0.5589%" height="15" fill="rgb(208,68,35)" fg:x="2026" fg:w="81"/><text x="14.2301%" y="127.50"></text></g><g><title>rust-1brc`DYLD-STUB$$memcmp (455 samples, 3.14%)</title><rect x="14.5391%" y="117" width="3.1397%" height="15" fill="rgb(232,128,0)" fg:x="2107" fg:w="455"/><text x="14.7891%" y="127.50">rus..</text></g><g><title>rust-1brc`rust_1brc::StationData::parse_data (63 samples, 0.43%)</title><rect x="17.6856%" y="117" width="0.4347%" height="15" fill="rgb(207,160,47)" fg:x="2563" fg:w="63"/><text x="17.9356%" y="127.50"></text></g><g><title>rust-1brc`std::io::append_to_string (97 samples, 0.67%)</title><rect x="18.1203%" y="117" width="0.6693%" height="15" fill="rgb(228,23,34)" fg:x="2626" fg:w="97"/><text x="18.3703%" y="127.50"></text></g><g><title>libsystem_malloc.dylib`_malloc_zone_malloc (250 samples, 1.73%)</title><rect x="51.4215%" y="101" width="1.7251%" height="15" fill="rgb(218,30,26)" fg:x="7452" fg:w="250"/><text x="51.6715%" y="111.50"></text></g><g><title>libsystem_malloc.dylib`_nanov2_free (388 samples, 2.68%)</title><rect x="53.1466%" y="101" width="2.6773%" height="15" fill="rgb(220,122,19)" fg:x="7702" fg:w="388"/><text x="53.3966%" y="111.50">li..</text></g><g><title>libsystem_malloc.dylib`small_free_scan_madvise_free (15 samples, 0.10%)</title><rect x="55.8446%" y="85" width="0.1035%" height="15" fill="rgb(250,228,42)" fg:x="8093" fg:w="15"/><text x="56.0946%" y="95.50"></text></g><g><title>libsystem_kernel.dylib`madvise (15 samples, 0.10%)</title><rect x="55.8446%" y="69" width="0.1035%" height="15" fill="rgb(240,193,28)" fg:x="8093" fg:w="15"/><text x="56.0946%" y="79.50"></text></g><g><title>libsystem_malloc.dylib`free_small (39 samples, 0.27%)</title><rect x="55.8239%" y="101" width="0.2691%" height="15" fill="rgb(216,20,37)" fg:x="8090" fg:w="39"/><text x="56.0739%" y="111.50"></text></g><g><title>libsystem_malloc.dylib`small_madvise_free_range_no_lock (21 samples, 0.14%)</title><rect x="55.9481%" y="85" width="0.1449%" height="15" fill="rgb(206,188,39)" fg:x="8108" fg:w="21"/><text x="56.1981%" y="95.50"></text></g><g><title>libsystem_kernel.dylib`madvise (20 samples, 0.14%)</title><rect x="55.9550%" y="69" width="0.1380%" height="15" fill="rgb(217,207,13)" fg:x="8109" fg:w="20"/><text x="56.2050%" y="79.50"></text></g><g><title>libsystem_malloc.dylib`mvm_deallocate_plat (5 samples, 0.03%)</title><rect x="56.0999%" y="101" width="0.0345%" height="15" fill="rgb(231,73,38)" fg:x="8130" fg:w="5"/><text x="56.3499%" y="111.50"></text></g><g><title>libsystem_kernel.dylib`_kernelrpc_mach_vm_deallocate_trap (5 samples, 0.03%)</title><rect x="56.0999%" y="85" width="0.0345%" height="15" fill="rgb(225,20,46)" fg:x="8130" fg:w="5"/><text x="56.3499%" y="95.50"></text></g><g><title>libsystem_malloc.dylib`nanov2_malloc (302 samples, 2.08%)</title><rect x="56.1344%" y="101" width="2.0839%" height="15" fill="rgb(210,31,41)" fg:x="8135" fg:w="302"/><text x="56.3844%" y="111.50">l..</text></g><g><title>libsystem_platform.dylib`_platform_memmove (245 samples, 1.69%)</title><rect x="58.2183%" y="101" width="1.6906%" height="15" fill="rgb(221,200,47)" fg:x="8437" fg:w="245"/><text x="58.4683%" y="111.50"></text></g><g><title>libsystem_malloc.dylib`mvm_allocate_pages_plat (2 samples, 0.01%)</title><rect x="59.9227%" y="53" width="0.0138%" height="15" fill="rgb(226,26,5)" fg:x="8684" fg:w="2"/><text x="60.1727%" y="63.50"></text></g><g><title>libsystem_kernel.dylib`_kernelrpc_mach_vm_map_trap (2 samples, 0.01%)</title><rect x="59.9227%" y="37" width="0.0138%" height="15" fill="rgb(249,33,26)" fg:x="8684" fg:w="2"/><text x="60.1727%" y="47.50"></text></g><g><title>rust-1brc`&lt;alloc::string::String as core::clone::Clone&gt;::clone (5 samples, 0.03%)</title><rect x="59.9158%" y="101" width="0.0345%" height="15" fill="rgb(235,183,28)" fg:x="8683" fg:w="5"/><text x="60.1658%" y="111.50"></text></g><g><title>libsystem_malloc.dylib`szone_malloc_should_clear (5 samples, 0.03%)</title><rect x="59.9158%" y="85" width="0.0345%" height="15" fill="rgb(221,5,38)" fg:x="8683" fg:w="5"/><text x="60.1658%" y="95.50"></text></g><g><title>libsystem_malloc.dylib`small_malloc_should_clear (4 samples, 0.03%)</title><rect x="59.9227%" y="69" width="0.0276%" height="15" fill="rgb(247,18,42)" fg:x="8684" fg:w="4"/><text x="60.1727%" y="79.50"></text></g><g><title>libsystem_malloc.dylib`small_malloc_from_free_list (2 samples, 0.01%)</title><rect x="59.9365%" y="53" width="0.0138%" height="15" fill="rgb(241,131,45)" fg:x="8686" fg:w="2"/><text x="60.1865%" y="63.50"></text></g><g><title>rust-1brc`DYLD-STUB$$malloc (35 samples, 0.24%)</title><rect x="59.9503%" y="101" width="0.2415%" height="15" fill="rgb(249,31,29)" fg:x="8688" fg:w="35"/><text x="60.2003%" y="111.50"></text></g><g><title>rust-1brc`DYLD-STUB$$memcpy (13 samples, 0.09%)</title><rect x="60.1918%" y="101" width="0.0897%" height="15" fill="rgb(225,111,53)" fg:x="8723" fg:w="13"/><text x="60.4418%" y="111.50"></text></g><g><title>rust-1brc`__rdl_alloc (34 samples, 0.23%)</title><rect x="60.2815%" y="101" width="0.2346%" height="15" fill="rgb(238,160,17)" fg:x="8736" fg:w="34"/><text x="60.5315%" y="111.50"></text></g><g><title>rust-1brc`alloc::vec::Vec&lt;T,A&gt;::extend_from_slice (76 samples, 0.52%)</title><rect x="60.5161%" y="101" width="0.5244%" height="15" fill="rgb(214,148,48)" fg:x="8770" fg:w="76"/><text x="60.7661%" y="111.50"></text></g><g><title>rust-1brc`core::num::dec2flt::_&lt;impl core::str::traits::FromStr for f64&gt;::from_str (124 samples, 0.86%)</title><rect x="61.0406%" y="101" width="0.8556%" height="15" fill="rgb(232,36,49)" fg:x="8846" fg:w="124"/><text x="61.2906%" y="111.50"></text></g><g><title>rust-1brc`core::str::converts::from_utf8 (26 samples, 0.18%)</title><rect x="61.8962%" y="101" width="0.1794%" height="15" fill="rgb(209,103,24)" fg:x="8970" fg:w="26"/><text x="62.1462%" y="111.50"></text></g><g><title>rust-1brc`core::str::pattern::StrSearcher::new (97 samples, 0.67%)</title><rect x="62.0756%" y="101" width="0.6693%" height="15" fill="rgb(229,88,8)" fg:x="8996" fg:w="97"/><text x="62.3256%" y="111.50"></text></g><g><title>libsystem_platform.dylib`_platform_memcmp (112 samples, 0.77%)</title><rect x="68.6241%" y="85" width="0.7728%" height="15" fill="rgb(213,181,19)" fg:x="9945" fg:w="112"/><text x="68.8741%" y="95.50"></text></g><g><title>rust-1brc`DYLD-STUB$$memcmp (53 samples, 0.37%)</title><rect x="69.3969%" y="85" width="0.3657%" height="15" fill="rgb(254,191,54)" fg:x="10057" fg:w="53"/><text x="69.6469%" y="95.50"></text></g><g><title>rust-1brc`core::num::dec2flt::_&lt;impl core::str::traits::FromStr for f64&gt;::from_str (613 samples, 4.23%)</title><rect x="69.7626%" y="85" width="4.2299%" height="15" fill="rgb(241,83,37)" fg:x="10110" fg:w="613"/><text x="70.0126%" y="95.50">rust-..</text></g><g><title>rust-1brc`core::num::dec2flt::parse::parse_number (425 samples, 2.93%)</title><rect x="71.0599%" y="69" width="2.9327%" height="15" fill="rgb(233,36,39)" fg:x="10298" fg:w="425"/><text x="71.3099%" y="79.50">ru..</text></g><g><title>rust-1brc`core::num::dec2flt::parse::parse_number (20 samples, 0.14%)</title><rect x="73.9925%" y="85" width="0.1380%" height="15" fill="rgb(226,3,54)" fg:x="10723" fg:w="20"/><text x="74.2425%" y="95.50"></text></g><g><title>rust-1brc`rust_1brc::StationData::parse_data (2,056 samples, 14.19%)</title><rect x="62.7450%" y="101" width="14.1871%" height="15" fill="rgb(245,192,40)" fg:x="9093" fg:w="2056"/><text x="62.9950%" y="111.50">rust-1brc`rust_1brc::S..</text></g><g><title>rust-1brc`core::str::pattern::StrSearcher::new (406 samples, 2.80%)</title><rect x="74.1306%" y="85" width="2.8015%" height="15" fill="rgb(238,167,29)" fg:x="10743" fg:w="406"/><text x="74.3806%" y="95.50">ru..</text></g><g><title>libsystem_kernel.dylib`read (191 samples, 1.32%)</title><rect x="78.6020%" y="85" width="1.3180%" height="15" fill="rgb(232,182,51)" fg:x="11391" fg:w="191"/><text x="78.8520%" y="95.50"></text></g><g><title>libsystem_platform.dylib`_platform_memmove (246 samples, 1.70%)</title><rect x="79.9200%" y="85" width="1.6975%" height="15" fill="rgb(231,60,39)" fg:x="11582" fg:w="246"/><text x="80.1700%" y="95.50"></text></g><g><title>rust-1brc`DYLD-STUB$$memcpy (39 samples, 0.27%)</title><rect x="81.6243%" y="85" width="0.2691%" height="15" fill="rgb(208,69,12)" fg:x="11829" fg:w="39"/><text x="81.8743%" y="95.50"></text></g><g><title>rust-1brc`alloc::vec::Vec&lt;T,A&gt;::extend_from_slice (81 samples, 0.56%)</title><rect x="81.8935%" y="85" width="0.5589%" height="15" fill="rgb(235,93,37)" fg:x="11868" fg:w="81"/><text x="82.1435%" y="95.50"></text></g><g><title>rust-1brc`core::slice::memchr::memchr_aligned (1,552 samples, 10.71%)</title><rect x="82.4524%" y="85" width="10.7094%" height="15" fill="rgb(213,116,39)" fg:x="11949" fg:w="1552"/><text x="82.7024%" y="95.50">rust-1brc`core::..</text></g><g><title>rust-1brc`std::io::append_to_string (3,336 samples, 23.02%)</title><rect x="76.9321%" y="101" width="23.0196%" height="15" fill="rgb(222,207,29)" fg:x="11149" fg:w="3336"/><text x="77.1821%" y="111.50">rust-1brc`std::io::append_to_string</text></g><g><title>rust-1brc`core::str::converts::from_utf8 (984 samples, 6.79%)</title><rect x="93.1617%" y="85" width="6.7900%" height="15" fill="rgb(206,96,30)" fg:x="13501" fg:w="984"/><text x="93.4117%" y="95.50">rust-1brc..</text></g><g><title>rust-1brc`std::sync::mpmc::Sender&lt;T&gt;::send (2 samples, 0.01%)</title><rect x="99.9517%" y="101" width="0.0138%" height="15" fill="rgb(218,138,4)" fg:x="14485" fg:w="2"/><text x="100.2017%" y="111.50"></text></g><g><title>rust-1brc`std::sync::mpmc::list::Channel&lt;T&gt;::recv (4 samples, 0.03%)</title><rect x="99.9655%" y="101" width="0.0276%" height="15" fill="rgb(250,191,14)" fg:x="14487" fg:w="4"/><text x="100.2155%" y="111.50"></text></g><g><title>all (14,492 samples, 100%)</title><rect x="0.0000%" y="197" width="100.0000%" height="15" fill="rgb(239,60,40)" fg:x="0" fg:w="14492"/><text x="0.2500%" y="207.50"></text></g><g><title>libsystem_pthread.dylib`thread_start (14,490 samples, 99.99%)</title><rect x="0.0138%" y="181" width="99.9862%" height="15" fill="rgb(206,27,48)" fg:x="2" fg:w="14490"/><text x="0.2638%" y="191.50">libsystem_pthread.dylib`thread_start</text></g><g><title>libsystem_pthread.dylib`_pthread_start (14,490 samples, 99.99%)</title><rect x="0.0138%" y="165" width="99.9862%" height="15" fill="rgb(225,35,8)" fg:x="2" fg:w="14490"/><text x="0.2638%" y="175.50">libsystem_pthread.dylib`_pthread_start</text></g><g><title>rust-1brc`std::sys::pal::unix::thread::Thread::new::thread_start (14,490 samples, 99.99%)</title><rect x="0.0138%" y="149" width="99.9862%" height="15" fill="rgb(250,213,24)" fg:x="2" fg:w="14490"/><text x="0.2638%" y="159.50">rust-1brc`std::sys::pal::unix::thread::Thread::new::thread_start</text></g><g><title>rust-1brc`core::ops::function::FnOnce::call_once{{vtable.shim}} (14,490 samples, 99.99%)</title><rect x="0.0138%" y="133" width="99.9862%" height="15" fill="rgb(247,123,22)" fg:x="2" fg:w="14490"/><text x="0.2638%" y="143.50">rust-1brc`core::ops::function::FnOnce::call_once{{vtable.shim}}</text></g><g><title>rust-1brc`std::sys_common::backtrace::__rust_begin_short_backtrace (11,769 samples, 81.21%)</title><rect x="18.7897%" y="117" width="81.2103%" height="15" fill="rgb(231,138,38)" fg:x="2723" fg:w="11769"/><text x="19.0397%" y="127.50">rust-1brc`std::sys_common::backtrace::__rust_begin_short_backtrace</text></g></svg></svg>